package main

// The main Go binary has a sub-command called generate which is used to trigger code generation tools (like stringer).

// execute this code generate in the terminal just typing `go generate`
// or to execute all go:generate for all sub-packages: $ go generate ./...

//go:generate echo "Hello World"
func main() {

}
